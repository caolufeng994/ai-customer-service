"""
Agent 核心层：意图识别 + 策略路由。

在单意图 RAG 主链路之上增加一层轻量、确定性的意图门控：
- 知识类意图（产品咨询/价格套餐/退款售后/账号登录/知识库文档/订单）→ RAG 主链路
- 兜底/未知意图（闲聊/天气/越界等）→ 无上下文兜底提示，不注入 RAG 内容

设计原则：规则分类器零额外 LLM 调用（满足 AI架构设计.md 的成本控制），
可选 LLM 兜底默认关闭；路由为纯函数单分发，无循环/遗漏风险。
"""
