# 运行指南

## 环境要求

### 系统要求
- **操作系统**: Windows 10+, macOS 10.15+, Linux
- **Python**: 3.9+
- **Node.js**: 16+
- **MySQL**: 8.0+
- **内存**: 至少4GB（推荐8GB）
- **磁盘**: 至少10GB可用空间

### 外部依赖
- **DashScope API Key**: 用于LLM和Embedding（可选，可使用本地模型）
- **Ollama**: 用于本地LLM（可选，需单独安装）

## 快速开始

### 1. 克隆项目

```bash
git clone <repository-url>
cd 5day
```

### 2. 后端设置

#### 2.1 创建虚拟环境

```bash
cd backend
python -m venv venv

# Windows
venv\Scripts\activate

# macOS/Linux
source venv/bin/activate
```

#### 2.2 安装依赖

```bash
pip install -r requirements.txt
```

#### 2.3 配置环境变量

复制 `.env.example` 为 `.env` 并配置：

```bash
cp .env.example .env
```

编辑 `.env` 文件（以下为与代码一致的最小可运行配置，完整模板见仓库 `backend/.env.example`）：

```env
# 数据库 (MySQL 8.0，应用不会自动建表，需先执行 backend/init_db.sql)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ai_customer_service
DB_USER=root
DB_PASSWORD=your_password_here

# JWT（务必改为随机长串：python -c "import secrets; print(secrets.token_urlsafe(48))")
JWT_SECRET_KEY=your-secret-key-change-this-in-production

# LLM（阿里云百炼，DashScope）
LLM_PROVIDER=dashscope
DASHSCOPE_API_KEY=your-dashscope-api-key      # 同时供 LLM 与 Embedding 使用（仅一个 Key）
DASHSCOPE_MODEL=qwen3.7-plus                  # 见下方「模型选择指南」

# 本地降级模型（仅 LLM_PROVIDER=ollama 时生效）
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b

# Embedding（与 LLM 共用同一 Key）
EMBEDDING_PROVIDER=dashscope
EMBEDDING_MODEL=qwen3.7-text-embedding        # 最新嵌入模型，1024 维

# 向量库（Chroma，本地文件型，首次运行自动创建）
CHROMA_PERSIST_DIR=./data/chroma

# 知识库初始化（可选）：true=服务启动时自动向量化 seed_docs（幂等）；默认 false，改用 init_kb.py 手动初始化
AUTO_INIT_KB=false

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
```

> ⚠️ 注意：真实密钥请写入 `.env`（已被 `.gitignore` 排除，**严禁提交**）；仓库内只保留 `backend/.env.example` 模板。

#### 2.4 初始化数据库

```bash
# 登录MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE ai_customer_service CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 执行初始化脚本
USE ai_customer_service;
SOURCE init_db.sql;
```

#### 2.5 初始化知识库（种子文档向量化，首次必做）

题目要求「初始化时自动向量化，启动即可测问答」。仓库已在 `seed_docs/` 预置 3 篇示例文档（产品介绍 / 常见问题FAQ / 退换货政策）。执行下面的命令，将它们经由标准 RAG 流水线（加载→切分→嵌入→写入向量库）导入知识库：

```bash
# 必须在 backend/ 目录下运行，确保 ./uploads 与 ./data/chroma 与服务端一致
cd backend
python init_kb.py            # 幂等：已入库(ready)的文档会自动跳过
python init_kb.py --force    # 强制重建：先清除旧向量再重新向量化
```

前置条件与启动服务相同：MySQL 已执行 `init_db.sql`、`.env` 已配置 `DASHSCOPE_API_KEY`。脚本会打印新入库/跳过/失败的文档数量及向量总数。

> 💡 **可选：启动即自动初始化。** 若希望「服务一启动就自动完成向量化」，在 `.env` 中设置 `AUTO_INIT_KB=true`，服务启动时会自动、幂等地执行上述种子导入（默认 `false`，避免每次启动都触发网络 Embedding 调用）。手动运行 `init_kb.py` 与该开关二选一即可。

#### 2.6 启动后端

```bash
python -m app.main
```

后端将在 `http://localhost:8000` 启动。

API文档地址：`http://localhost:8000/docs`

### 3. 前端设置

#### 3.1 安装依赖

```bash
cd frontend
npm install
```

#### 3.2 启动开发服务器

```bash
npm run dev
```

前端将在 `http://localhost:5173` 启动。

### 4. Spike测试（可选）

在配置外部依赖前，运行Spike测试验证连接：

```bash
cd spike
pip install -r requirements.txt
python test_llm.py
python test_embedding.py
python test_chroma.py
```

## 详细配置

### MySQL配置

#### 创建数据库用户（推荐）

```sql
CREATE USER 'ai_cs_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON ai_customer_service.* TO 'ai_cs_user'@'localhost';
FLUSH PRIVILEGES;
```

#### 更新 `.env` 数据库URL

```env
DATABASE_URL=mysql+pymysql://ai_cs_user:your_password@localhost:3306/ai_customer_service
```

### DashScope配置

1. 访问 [阿里云百炼平台](https://bailian.console.aliyun.com/)
2. 创建 API Key（注意：**LLM 与 Embedding 共用同一个 Key**，无需分别申请）
3. 在 `.env` 中配置 `DASHSCOPE_API_KEY`
4. 选择对话模型 `DASHSCOPE_MODEL` 与嵌入模型 `EMBEDDING_MODEL`（见下方「模型选择指南」）

### 模型选择指南

本项目默认使用阿里云百炼较新的 **Qwen3.7** 系列模型，可通过环境变量自由切换。

**1. 对话模型（`DASHSCOPE_MODEL`）**

| 模型 | 特点 | 适用场景 |
| --- | --- | --- |
| `qwen3.7-max` | 最新旗舰，能力最强、成本最高 | 对回答质量要求极高的场景 |
| `qwen3.7-plus`（默认） | 质量与成本均衡 | 大多数客服问答场景 |
| `qwen3.7-flash` | 速度最快、成本最低 | 高并发 / 低延迟需求 |

**2. 嵌入模型（`EMBEDDING_MODEL`）**

- 默认 `qwen3.7-text-embedding`（2026 发布，1024 维）。该维度与 Chroma 集合维度一致，可直接使用，无需改动向量库结构。
- 如需离线，可设 `EMBEDDING_PROVIDER=local` 并指定 `LOCAL_EMBEDDING_MODEL`（如 `bge-small-zh-v1.5`），但会引入 `sentence-transformers` 较重的本地依赖。

**3. 切换为本地模型（可选兜底）**

- 设 `LLM_PROVIDER=ollama`，安装 Ollama 后 `ollama pull qwen2.5:7b`（或更新的 `qwen3` 系列）。
- 代码在 DashScope 调用异常时会自动 `fallback_to_ollama()` 降级，前提是本机已安装并拉取对应模型。

> 模型名以阿里云百炼控制台「模型广场」当前可调用列表为准；若某模型名不可用，接口会返回 404，更换为可用模型名即可。

### Ollama配置（本地模型）

#### 安装Ollama

```bash
# macOS/Linux
curl -fsSL https://ollama.com/install.sh | sh

# Windows
# 从 https://ollama.com/download 下载安装包
```

#### 拉取模型

```bash
ollama pull qwen
```

#### 更新 `.env`

```env
LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen
```

### Chroma配置

Chroma 为本地文件型向量库，无需独立服务、无需账号密码，首次运行在 `CHROMA_PERSIST_DIR` 指定的目录自动创建（默认 `./data/chroma`）：

```env
CHROMA_PERSIST_DIR=./data/chroma
```

## 生产部署

### 后端部署

#### 使用Gunicorn

```bash
pip install gunicorn
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000
```

#### 使用Systemd（Linux）

创建 `/etc/systemd/system/ai-cs.service`：

```ini
[Unit]
Description=AI Customer Service Backend
After=network.target

[Service]
Type=notify
User=www-data
WorkingDirectory=/path/to/backend
Environment="PATH=/path/to/backend/venv/bin"
ExecStart=/path/to/backend/venv/bin/gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000
Restart=always

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl start ai-cs
sudo systemctl enable ai-cs
```

### 前端部署

#### 构建生产版本

```bash
cd frontend
npm run build
```

#### 使用Nginx

配置 `/etc/nginx/sites-available/ai-cs`：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端API代理
    location /api {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # SSE支持
        proxy_buffering off;
        proxy_cache off;
        proxy_set_header Connection '';
        chunked_transfer_encoding on;
    }
}
```

启用配置：

```bash
sudo ln -s /etc/nginx/sites-available/ai-cs /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### Docker部署（可选）

#### 后端Dockerfile

```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["gunicorn", "app.main:app", "-w", "4", "-k", "uvicorn.workers.UvicornWorker", "-b", "0.0.0.0:8000"]
```

#### 前端Dockerfile

```dockerfile
FROM node:16-alpine as builder

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

#### docker-compose.yml

```yaml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: ai_customer_service
    volumes:
      - mysql_data:/var/lib/mysql
      - ./backend/init_db.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "3306:3306"

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: mysql+pymysql://root:rootpassword@mysql:3306/ai_customer_service
    depends_on:
      - mysql
    volumes:
      - chroma_data:/app/chroma_data
      - ./backend/uploads:/app/uploads

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mysql_data:
  chroma_data:
```

启动：

```bash
docker-compose up -d
```

## 常见问题

### 1. 数据库连接失败

**问题**: `Can't connect to MySQL server`

**解决**:
- 检查MySQL是否启动
- 检查 `.env` 中的数据库URL是否正确
- 检查防火墙设置

### 2. 向量化失败

**问题**: `Failed to embed text`

**解决**:
- 检查DashScope API Key是否正确
- 检查网络连接
- 尝试切换到本地模型

### 3. Chroma存储错误

**问题**: `Failed to initialize vector store`

**解决**:
- 检查 `CHROMA_PERSIST_DIR` 目录权限
- 删除 `chroma_data` 目录重新初始化

### 4. 前端CORS错误

**问题**: `CORS policy error`

**解决**:
- 检查 `.env` 中的 `CORS_ORIGINS` 配置
- 确保前端URL已添加到允许列表

### 5. SSE连接断开

**问题**: SSE流中断

**解决**:
- 检查Nginx配置（生产环境）
- 确保 `proxy_buffering off` 已配置
- 检查超时设置

## 监控与日志

### 后端日志

日志输出到控制台，可通过以下方式查看：

```bash
# 开发环境
python -m app.main

# 生产环境
journalctl -u ai-cs -f
```

### 前端日志

浏览器控制台查看，生产环境可配置日志级别。

### 数据库日志

```sql
-- 查看慢查询
SHOW VARIABLES LIKE 'slow_query_log%';

-- 启用慢查询日志
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 2;
```

## 备份与恢复

### 数据库备份

```bash
mysqldump -u root -p ai_customer_service > backup_$(date +%Y%m%d).sql
```

### 数据库恢复

```bash
mysql -u root -p ai_customer_service < backup_20240101.sql
```

### Chroma向量备份

```bash
# 备份
tar -czf chroma_backup_$(date +%Y%m%d).tar.gz chroma_data/

# 恢复
tar -xzf chroma_backup_20240101.tar.gz
```

## 性能优化

### 后端优化

1. **数据库连接池**: 调整SQLAlchemy连接池大小
2. **缓存**: 添加Redis缓存常用查询
3. **异步处理**: 使用Celery处理耗时任务

### 前端优化

1. **代码分割**: 使用React.lazy()
2. **CDN**: 静态资源CDN加速
3. **压缩**: Gzip压缩

## 安全建议

1. **环境变量文件（关键）**:
   - ✅ **必须提交 `backend/.env.example`** 到仓库——它是所有必需环境变量的「说明书」，审阅者/协作者据此知道要配哪些项。
   - ⛔ **严禁提交 `.env`**（含真实 `DASHSCOPE_API_KEY`、`DB_PASSWORD`、`JWT_SECRET_KEY`）。本仓库 `.gitignore` 已排除 `.env` / `.env.*` / `*.env`，并显式保留 `!.env.example`；提交前请确认 `git status` 中不会出现 `.env`。
   - 推送前可用 `git log -p --all -- backend/.env` 自检，确认历史中无密钥泄露；若曾误提交，立即重置 Key 并清理历史。
2. **API Key**: 定期更换 API Key；不要在代码、日志、文档中硬编码密钥。
3. **HTTPS**: 生产环境使用 HTTPS。
4. **防火墙**: 限制数据库访问，避免 MySQL 端口暴露公网。
5. **定期备份**: 定期备份 MySQL 数据与 Chroma 向量目录。

## 开发工具

### API测试

使用FastAPI自动生成的文档：`http://localhost:8000/docs`

### 数据库管理

推荐工具：
- DBeaver
- MySQL Workbench
- phpMyAdmin

### 向量数据查看

Chroma提供Web UI，可通过以下方式启动：

```python
import chromadb
client = chromadb.PersistentClient(path="./chroma_data")
# 访问 http://localhost:8000 (需要额外配置)
```

## 更新升级

### 后端更新

```bash
cd backend
git pull
pip install -r requirements.txt --upgrade
python -m app.main
```

### 前端更新

```bash
cd frontend
git pull
npm install
npm run build
```

### 数据库迁移

如需修改数据库结构，使用Alembic：

```bash
cd backend
alembic revision --autogenerate -m "description"
alembic upgrade head
```

## 技术支持

如遇到问题，请检查：
1. 日志文件
2. 环境变量配置
3. 依赖版本兼容性
4. 网络连接

更多帮助请参考项目文档或提交Issue。
