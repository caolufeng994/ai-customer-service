# AI 智能客服系统 · 五日交付执行方案

> 依据《AI 开发工程师笔试题》全文分析产出。适用对象：单人独立开发（可扩展为 2 人）。
> 周期：收到题目起第 1 天 ~ 第 5 天 23:59。交付形态：Git 仓库 + 文档包 + 邮件提交。

---

## 一、项目全景与目标反推

### 1.1 题目本质

这不是一道"功能实现题"，而是一道**AI 工程能力鉴别题**。从评分权重可以直接反推投入配比：

| 考察维度 | 权重 | 本质在考什么 | 时间应占比 |
|---|---|---|---|
| AI 链路完整性 | 40% | RAG 五环（上传→向量化→检索→生成→流式）是否真的跑通，引用来源是否准确 | 40% |
| AI 工程思维 | 25% | Prompt 设计、幻觉抑制、空检索兜底、上下文截断策略 | 25%（含文档） |
| 代码质量 | 20% | 分层清晰、异常处理、注释 | 15% |
| 数据库与 API 设计 | 15% | 表结构合理、向量元数据关联、API 规范 | 10% |

**关键判断**：`AI 链路完整性 + AI 工程思维 = 65%`。前端界面在评分表中**没有独立分项**，仅通过"流式体验流畅"作为加分项体现。因此前端投入必须严格压缩，绝不允许美化界面挤占 RAG 链路时间。

### 1.2 硬性交付物核对表（缺一即失分）

| # | 交付物 | 强制/可选 | 验收标准 |
|---|---|---|---|
| D1 | `backend/`（源码 + 建表脚本 + requirements.txt + README） | 强制 | 按 README 可从零启动 |
| D2 | `frontend/`（源码 + package.json + README） | 强制 | `npm i && npm run dev` 可跑 |
| D3 | `docs/API文档.md` | 强制 | 含登录、上传、问答（流式）、会话历史、反馈；含请求/响应示例；含 SSE 格式说明 |
| D4 | `docs/数据库设计.md` | 强制 | ER 图 + 表结构说明 |
| D5 | `docs/AI架构设计.md` | 强制 | RAG 流程图 + Prompt 模板 + 检索策略（阈值/Top-K 及理由） |
| D6 | `docs/业务流程说明.md` | 强制 | 问答完整链路图 |
| D7 | `项目说明.md` | 强制 | 技术选型理由 + 架构图 + **AI 工程问题思考（重点）** + AI 工具使用体会 |
| D8 | `运行指南.md` | 强制 | 含模型/API 配置 |
| D9 | `.env.example` | 强制 | 真实 Key 严禁入库 |
| D10 | 预置 2-3 篇知识文档（2000-5000 字），启动即可问答 | 强制 | 冷启动无需人工上传 |
| D11 | 邮件至 `xzjl@haici.com`，备注**院校 + 姓名** | 强制 | 第 5 天 23:59 前 |

> ⚠️ 高频失分点：`.env.example` 缺失、真实 API Key 被 commit、邮件忘记备注院校姓名、初始化数据未预置导致面试官打开就是空库。

---

## 二、需求拆解与优先级排序

采用 **MoSCoW + 评分锚定**，划为 P0/P1/P2 三档。**P0 未全绿之前，禁止触碰 P1；P1 未完成，禁止触碰 P2。**

### 2.1 P0 —— 必做主线（对应 65% 核心分 + 15% 设计分）

| ID | 模块 | 任务 | 交付判定（DoD） |
|---|---|---|---|
| P0-1 | 基建 | 项目骨架、配置中心、日志、统一异常、统一响应体 | `/health` 返回 200 |
| P0-2 | 数据库 | 8 张表建模 + 初始化脚本 + 种子数据 | `init_db.sql` 一键建库 |
| P0-3 | 用户与会话 | 注册/登录（JWT）、创建 Session、会话列表、会话详情 | 5 个接口全通 |
| P0-4 | 知识库入库 | 上传 txt/md → 解析 → 切分 → Embedding → 向量库写入 → 状态机流转 | 上传后 status 从 processing→ready |
| P0-5 | 知识库管理 | 列表（名称/时间/状态）、删除（级联清向量） | 删除后检索不再命中该文档 |
| P0-6 | 向量检索 | Top-K + 相似度阈值 + metadata 过滤 | 检索返回 chunk + score + source |
| P0-7 | RAG 生成 | Prompt 组装 → LLM 调用 → 引用来源抽取 | 回答附带「文档名 + 片段摘要」 |
| P0-8 | 流式输出 | SSE 逐字下发，前端逐字渲染 | 首 token < 2s，无整段闪现 |
| P0-9 | 多轮对话 | 携带最近 N 轮历史（N 可配） | 指代问题（"它多少钱"）能答对 |
| P0-10 | 业务规则 | 提问 ≤500 字、空检索兜底、每日 100 次配额（可配） | 三条规则均有测试用例 |
| P0-11 | 反馈 | 点赞/踩 + 文字反馈 | 落库并可在详情中回显 |
| P0-12 | 异常处理 | LLM 超时、限流、向量库异常、解析失败 | 均有降级路径，不 500 裸奔 |
| P0-13 | 前端 | 登录页、会话页（流式对话 + 引用卡片）、知识库页 | 3 个页面可用即可，不追求精美 |
| P0-14 | 文档 | D3~D8 六份文档 | 全部完成 |

### 2.2 P1 —— 高性价比加分（评分表明确列出的加分项）

| ID | 任务 | 性价比 | 说明 |
|---|---|---|---|
| P1-1 | **大规模检索下的 LLM 执行保障**（防注意力稀释/防幻觉） | ★★★★★ | 题目用整段文字描述，是最被看重的加分项，且**允许只写文档**。见第九章 |
| P1-2 | **Agent 任务拆解设计**（终极挑战） | ★★★★★ | 明确"不要求实现完整代码"，纯设计即可拿分。见第九章 |
| P1-3 | 意图识别（产品咨询/售后/闲聊/投诉） | ★★★★ | 一次轻量 LLM 分类调用 + 会话记录标注，工作量约 2 小时 |
| P1-4 | 追问引导（2-3 条建议） | ★★★★ | 复用同一次生成的尾部 JSON 输出，几乎零额外成本 |
| P1-5 | Prompt 优化减少幻觉（含文档说明思路） | ★★★★★ | 与 P1-1 合并做，写清楚 A/B 对比 |
| P1-6 | 知识库增量更新（新增文档不影响已有向量） | ★★★★ | 架构上天然满足（按 doc_id 分区 upsert），只需文档说明 + 一个测试用例 |

### 2.3 P2 —— 有余力再做

| ID | 任务 | 说明 |
|---|---|---|
| P2-1 | 管理后台（会话记录、反馈统计、日均问答折线图） | 工作量大（约 1 天），性价比最低，建议仅做 3 个只读统计接口 + 一个 ECharts 页面 |
| P2-2 | 多知识库路由 | 后端预留 `kb_id` 字段与路由函数，先做"规则路由"，LLM 路由作为文档设计 |
| P2-3 | PDF 解析支持 | 题目明示可选；PyMuPDF 接入约 1 小时，建议做（成本低） |

---

## 三、技术架构方案

### 3.1 技术选型与理由（直接可写进《项目说明.md》）

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | React 18 + TypeScript + Vite + Ant Design | 题目建议方案之一；Vite 冷启动快；AntD 免写样式，把时间留给 AI 链路 |
| 流式接收 | `fetch` + `ReadableStream` 手动解析 SSE | **关键决策**：原生 `EventSource` 只支持 GET 且不能带自定义 Header，无法承载 JWT + 长 body 的问答请求。必须用 fetch 流式读取 |
| 后端 | Python 3.11 + FastAPI + Uvicorn | 原生 `StreamingResponse` 支持 SSE；async 天然适配 LLM 流式；Pydantic 自动生成 OpenAPI，可直接导出 API 文档 |
| ORM | SQLAlchemy 2.0 + Alembic | 标准方案，便于给出建表脚本 |
| LLM | 通义千问 `qwen-plus`（DashScope，OpenAI 兼容协议） | 有免费额度；中文能力强；兼容 OpenAI SDK，切换成本为零 |
| LLM 兜底 | 本地 Ollama（`qwen2.5:7b`） | 防止免费额度耗尽导致演示翻车 |
| Embedding | DashScope `text-embedding-v3`（1024 维）；本地 `bge-small-zh-v1.5` 作离线兜底 | 云端免部署、无 GPU 依赖、速度快；本地兜底保证断网可跑 |
| 向量库 | Chroma（本地持久化模式） | 无需独立服务；原生支持 metadata 过滤与按 id 删除，完美匹配"删除文档同步清向量""增量更新"两条需求 |
| 关系库 | MySQL 8.0 | 题目强制 |
| 缓存/配额 | 内存 TTL + MySQL 计数表（不引入 Redis） | 5 天周期内减少组件，降低"跑不起来"风险 |
| RAG 框架 | **不使用 LangChain，手写链路** | 题目 FAQ 明确："手动实现反而能更好展示对原理的理解"。手写检索器/Prompt 组装器/流式管道，每一步可控可讲 |

> 选型总原则：**在 5 天内，"能被面试官一键跑起来"比"技术栈时髦"重要一个数量级。** 每引入一个需要独立部署的中间件，演示失败概率就上升一档。

### 3.2 系统分层架构

```
┌──────────────────────────── 前端 React + TS ─────────────────────────────┐
│  登录页   │   会话页（流式渲染 + 引用卡片 + 追问按钮）  │  知识库页      │
└───────────────────────────┬──────────────────────────────────────────────┘
                            │  REST + SSE (fetch ReadableStream)
┌───────────────────────────▼──────────────────────────────────────────────┐
│  API 层 (FastAPI Router)                                                  │
│  auth.py │ session.py │ chat.py(SSE) │ knowledge.py │ feedback.py │ stat.py│
├──────────────────────────────────────────────────────────────────────────┤
│  Service 层（业务编排）                                                    │
│  AuthService │ ChatService │ KnowledgeService │ QuotaService              │
├──────────────────────────────────────────────────────────────────────────┤
│  RAG Core（本项目核心资产，独立包 rag/）                                   │
│  ┌────────────┬────────────┬────────────┬────────────┬─────────────────┐ │
│  │ Loader     │ Splitter   │ Embedder   │ Retriever  │ ContextBuilder  │ │
│  │ txt/md/pdf │ 中文递归切分│ 批量向量化 │ TopK+阈值  │ 分层摘要+预算控制│ │
│  ├────────────┼────────────┼────────────┼────────────┼─────────────────┤ │
│  │ IntentCls  │ QueryRewrite│ PromptBuilder │ LLMClient │ CitationParser │ │
│  └────────────┴────────────┴────────────┴────────────┴─────────────────┘ │
├──────────────────────────────────────────────────────────────────────────┤
│  Infra 层                                                                 │
│  MySQL(SQLAlchemy) │ Chroma(持久化) │ LLMProvider(DashScope/Ollama) │ Log │
└──────────────────────────────────────────────────────────────────────────┘
```

**LLMProvider 抽象**是必须的设计：定义 `chat_stream()` / `chat()` / `embed()` 三个方法，DashScope 与 Ollama 两个实现，通过 `.env` 的 `LLM_PROVIDER` 切换。这一条既是工程能力体现，也是演示保险。

### 3.3 数据库设计（8 张表）

| 表名 | 关键字段 | 设计要点 |
|---|---|---|
| `users` | id, phone, email, password_hash, salt, created_at | 手机号与邮箱各建唯一索引，允许其一为空 |
| `sessions` | id, user_id, title, intent_tag, msg_count, created_at, updated_at | title 由首条提问自动摘要生成；`idx(user_id, updated_at desc)` 支撑会话列表 |
| `messages` | id, session_id, role(user/assistant), content, intent, token_in, token_out, latency_ms, finish_reason, created_at | 记录 latency 与 token 便于写"性能验证"章节 |
| `message_citations` | id, message_id, doc_id, chunk_id, score, snippet | **引用来源独立成表**，而非塞进 JSON 字段——这是"向量元数据关联"考点的正面回答 |
| `kb_documents` | id, kb_id, name, file_path, file_type, size, char_count, chunk_count, status(processing/ready/failed), error_msg, created_at | 状态机四态齐全，failed 必须带 error_msg |
| `kb_chunks` | id, doc_id, chunk_index, content, char_count, vector_id, created_at | **关系库保存 chunk 正文 + 向量库主键映射**，使删除、对账、溯源都可做，避免"向量库是黑盒" |
| `feedbacks` | id, message_id, user_id, rating(1/-1), comment, created_at | 唯一索引 (message_id, user_id) 防重复 |
| `usage_quota` | id, user_id, date, ask_count | 唯一索引 (user_id, date)，`INSERT ... ON DUPLICATE KEY UPDATE` 原子自增 |

**一致性设计（易被追问）**：`kb_chunks.vector_id` 是 MySQL 与 Chroma 的唯一桥梁。删除文档时顺序为：① MySQL 事务内将 document 置 `deleting` ② 按 `vector_id` 批量删 Chroma ③ 删 `kb_chunks` ④ 删 `kb_documents`。任一步失败保留 `deleting` 状态，由启动时的对账任务重试。

### 3.4 API 清单

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/register` | 注册 |
| POST | `/api/auth/login` | 登录，返回 JWT |
| GET | `/api/sessions` | 会话列表（分页） |
| POST | `/api/sessions` | 新建会话 |
| GET | `/api/sessions/{id}/messages` | 会话详情（完整对话记录 + 引用） |
| **POST** | **`/api/chat/stream`** | **核心流式问答接口（SSE）** |
| POST | `/api/kb/documents` | 上传文档（multipart），异步向量化 |
| GET | `/api/kb/documents` | 知识库列表（含状态） |
| DELETE | `/api/kb/documents/{id}` | 删除文档（级联清向量） |
| POST | `/api/feedback` | 提交反馈 |
| GET | `/api/stats/overview` | （P2）统计概览 |

### 3.5 SSE 协议设计（写进 API 文档）

```
POST /api/chat/stream
Content-Type: application/json
Authorization: Bearer <jwt>
{ "session_id": 12, "question": "退换货要多久？" }

--- 响应 (text/event-stream) ---
event: meta
data: {"message_id":88,"intent":"售后问题","retrieved":3}

event: citation
data: {"doc_name":"退换货政策.txt","chunk_id":"c_17","score":0.82,"snippet":"..."}

event: delta
data: {"text":"根据"}

event: delta
data: {"text":"退换货政策"}

event: suggestion
data: {"items":["运费谁承担？","超过7天还能退吗？"]}

event: done
data: {"finish_reason":"stop","latency_ms":2140,"tokens":{"in":1203,"out":186}}

event: error
data: {"code":"LLM_TIMEOUT","message":"模型响应超时，请重试"}
```

设计要点：**引用先于正文下发**，前端可立即渲染来源卡片，感知上更快；`error` 作为独立事件而非 HTTP 状态码，因为流已开始后无法再改 HTTP Code。响应头需设 `Cache-Control: no-cache`、`X-Accel-Buffering: no`（防 Nginx 缓冲导致"假卡顿"）。

---

## 四、核心数据流程

### 4.1 知识入库流（异步）

```
上传文件
  → 校验（类型白名单 / 大小上限 10MB）
  → 落盘 uploads/{uuid}.{ext}
  → INSERT kb_documents(status=processing)   ← 立即返回 202，不阻塞请求
  → BackgroundTask:
      ① Loader 解析（txt/md 直读；pdf 用 PyMuPDF 逐页提取）
      ② Splitter 中文递归切分：优先按 \n\n → \n → 。！？ → ，切
         目标 500 字/块，重叠 80 字（保证跨块语义不断裂）
      ③ Embedder 批量向量化（batch=16，失败重试 3 次指数退避）
      ④ Chroma upsert，metadata = {doc_id, kb_id, doc_name, chunk_index}
      ⑤ INSERT kb_chunks（保存正文 + vector_id）
      ⑥ UPDATE status=ready, chunk_count=N
    异常 → UPDATE status=failed, error_msg=<具体原因>
```

> **增量更新天然成立**：向量以 `doc_id` 为 metadata 分区 upsert，新增文档不触碰已有向量；重传同名文档则先按 doc_id 删旧向量再写新的。这一段直接对应加分项"知识库支持增量更新"。

### 4.2 RAG 问答流（同步流式）

```
① 入参校验：question ≤ 500 字 → 否则 400
② 配额校验：usage_quota 今日 < DAILY_LIMIT(默认100) → 否则 429
③ 载入历史：最近 N=6 条消息（3 轮）
④ Query Rewrite：若历史非空且问题含指代（它/这个/那款），用小模型改写为独立问题
   —— 这是多轮 RAG 的关键，直接检索原问题"它多少钱"必然召回失败
⑤ Intent 分类：产品咨询/售后问题/闲聊/投诉（闲聊直接跳过检索，走轻量 Prompt）
⑥ 向量检索：embed(rewritten_query) → Chroma top_k=8
⑦ 阈值过滤：cosine similarity ≥ 0.35 保留
   ├─ 命中 0 条 → 走兜底话术，标记 finish_reason=no_context，不调用 LLM 生成事实
   └─ 命中 ≥1 条 → 继续
⑧ 上下文构建（Token 预算 = 模型上限 - 输出预留 - 历史 - System）
   ├─ 片段去重（同 doc 相邻 chunk 合并）
   ├─ 分层摘要（片段数 > 8 时启用，见 9.1）
   └─ 规则优先级排序（见 9.1）
⑨ Prompt 组装（System + 规则块 + 检索块[带编号] + 历史 + 当前问题）
⑩ LLM 流式调用 → 逐 token 通过 SSE 下发
⑪ 落库：messages + message_citations，usage_quota +1
⑫ 生成追问建议（复用同次输出的结构化尾部）
```

### 4.3 三个"AI 特有工程问题"的处理（题目重点要求）

| 问题 | 处理策略 |
|---|---|
| **检索为空** | 双重判定：Top-1 分数 < 阈值 **或** 过滤后为空 → 返回固定兜底话术「抱歉，我在当前知识库中没有找到相关信息。您可以换个说法，或联系人工客服。」**不调用 LLM 生成内容**，从源头杜绝编造。同时把该问题写入 `messages.finish_reason=no_context`，可用于后续知识盲区分析 |
| **上下文超长** | 三级压缩：① 历史消息滑动窗口（保最近 3 轮）+ 更早内容做一次性摘要 ② 检索片段按 Token 预算贪心装填，超出部分做分层摘要 ③ 硬上限保护：组装后仍超限则丢弃低分片段并在 SSE meta 中标注 `truncated=true` |
| **LLM 幻觉** | 四道防线：① System Prompt 强约束「只能依据【参考资料】回答，资料中没有的必须回答不知道」② 强制引用：要求每个结论标注 `[1][2]` 来源编号，后处理校验编号是否越界，越界即触发重试 ③ 空检索不生成 ④ 分步校验（见 9.1），对含数字/期限/金额的回答做一次规则回读 |

---

## 五、依赖关系与关键路径

```
[基建骨架]──┬──▶[DB建模]──▶[用户会话API]──────────────┐
            │                                          │
            └──▶[LLMProvider抽象]──▶[Embedder]──▶[入库链路]──▶[检索器]──▶[RAG生成]──▶[SSE流式]──▶[联调]──▶[文档]
                                                                                      ▲
[前端脚手架]──▶[Mock 数据页面]──▶[登录/知识库页]────────────────────────────────────┘
```

**关键路径（不可压缩）**：`LLMProvider → Embedder → 入库链路 → 检索器 → RAG 生成 → SSE 流式`。
任何一环延误都会顺延整个项目，因此 **Day 1 必须完成 LLM 与 Embedding 的连通性验证**（哪怕只是一个 15 行的脚本打通 API），把最大的不确定性提前引爆。

**可并行**：前端脚手架 + Mock 页面、文档中的"技术选型/数据库设计"章节、预置知识文档准备，均可在后端开发间隙穿插完成。

---

## 六、五日执行计划（半日粒度）

### Day 1 —— 地基与不确定性排除

| 时段 | 任务 | 交付物 | DoD |
|---|---|---|---|
| 上午 | ① 申请 DashScope Key，跑通 chat + embedding 最小脚本 ② 安装 Ollama 拉取 qwen2.5:7b 作兜底 ③ Chroma 本地写入/检索 Demo | `spike/` 三个验证脚本 | 三个脚本全部成功输出 |
| 下午 | ④ 数据库 8 张表建模，产出 `init_db.sql` + ER 图 ⑤ FastAPI 骨架（配置/日志/统一异常/统一响应） ⑥ 前端 Vite 脚手架 + 路由 + 请求封装 | `init_db.sql`、后端骨架、前端骨架 | `/health` 200；前端能起 |
| 晚上 | ⑦ 准备 3 篇预置知识文档（产品介绍/FAQ/退换货政策，共约 3500 字） ⑧ 写《数据库设计.md》 | `seed_docs/`、`docs/数据库设计.md` | 文档完成 |

> 🚩 **里程碑 M1（Day 1 结束）**：外部依赖全部验证可用，数据库设计定稿。
> 🔴 **红线**：若 Day 1 晚 22:00 LLM 或 Embedding 仍未打通，立即切换到 Ollama + 本地 bge 全离线方案，不再纠结云端。

### Day 2 —— 知识库入库链路（RAG 前半段）

| 时段 | 任务 | DoD |
|---|---|---|
| 上午 | 用户注册/登录（JWT）、会话 CRUD 四个接口 | Postman 全通 |
| 下午 | Loader + Splitter + Embedder 三件套；上传接口 + BackgroundTask 异步向量化 + 状态机 | 上传 txt/md 后 status 变 ready，Chroma 中可查到向量 |
| 晚上 | 知识库列表/删除接口 + 级联清向量；启动时自动导入 `seed_docs/` | 删除后检索不再命中；重启后预置文档自动就绪 |

> 🚩 **里程碑 M2（Day 2 结束）**：知识可入库、可管理、可检索到。**这是全项目风险最高的一段，必须当天结束。**

### Day 3 —— RAG 问答与流式（核心攻坚日）

| 时段 | 任务 | DoD |
|---|---|---|
| 上午 | Retriever（Top-K + 阈值 + metadata 过滤）、ContextBuilder（Token 预算 + 去重）、PromptBuilder（System 模板 v1） | 命令行可完成一次完整 RAG 问答 |
| 下午 | `/api/chat/stream` SSE 接口；LLM 流式调用；引用来源解析与落库；多轮历史注入 + Query Rewrite | `curl -N` 可看到逐条 event 流出 |
| 晚上 | 三条业务规则（500 字/兜底/配额）+ 全链路异常处理（超时、限流、重试退避）+ 反馈接口 | 断网、超时、超配额三个场景均有优雅返回 |

> 🚩 **里程碑 M3（Day 3 结束）**：**后端 P0 全部完成，RAG 主链路端到端跑通。** 此为项目生死线。
> 🔴 **红线**：Day 3 结束若 SSE 未通，Day 4 上午必须停止一切其他工作专攻此项。

### Day 4 —— 前端联调 + 加分项 + 调优

| 时段 | 任务 | DoD |
|---|---|---|
| 上午 | 前端三页面接入真实接口；流式逐字渲染（fetch + ReadableStream）；引用卡片；打字机效果 | 浏览器中逐字显示，无整段闪现 |
| **中午 12:00** | **🔒 主线功能 Freeze**：P0 未完成的项直接降级或砍掉，不再新增主线功能 | — |
| 下午 | P1 攻坚：意图识别 + 追问引导 + Prompt v2 优化（对比 v1 做幻觉 A/B） | 会话记录中出现意图标签；回答后出现 2-3 条追问 |
| 晚上 | 构建 25 条评测集并跑一轮；据结果调阈值（0.3/0.35/0.4 对比）与 Top-K（5/8/10 对比），记录数据 | 产出《调优记录》表格，含命中率/引用正确率/拒答正确率 |

> 🚩 **里程碑 M4（Day 4 结束）**：系统完整可演示，关键参数有数据支撑（而非拍脑袋）。

### Day 5 —— 文档、硬核加分与提交

| 时段 | 任务 | DoD |
|---|---|---|
| 上午 | 撰写《AI架构设计.md》（RAG Mermaid 流程图 + Prompt 模板全文 + 阈值/TopK 选择依据附实验数据）、《业务流程说明.md》 | 两份文档完成 |
| 下午 | 撰写《项目说明.md》：技术选型理由 + 架构图 + **AI 工程三问深度回答** + AI 工具使用体会；撰写《API文档.md》《运行指南.md》 | 四份文档完成 |
| 晚上 18:00 前 | **硬核加分两篇**：`docs/大规模检索执行保障设计.md`、`docs/Agent任务拆解设计.md`（见第九章） | 两份文档 + 若有余力的最小实现 |
| 晚上 20:00-22:00 | **净环境验收**：换目录 clone → 按运行指南从零启动 → 跑通全流程；`.env.example` 检查；`git log` 全文搜索 Key 泄漏；README 三份齐全 | 全绿 |
| 晚上 22:00-23:00 | 推送仓库、发送邮件（标题与正文备注**院校 + 姓名**） | 邮件已发出 |

> 🚩 **里程碑 M5（Day 5 23:00）**：交付完成，留 1 小时缓冲。
> 🔴 **红线**：绝不允许拖到 23:30 之后提交。

---

## 七、资源分配建议

### 7.1 单人配置（默认）

| 资源项 | 分配 |
|---|---|
| 总工时 | 5 天 × 12h ≈ 60h（含缓冲 8h） |
| 后端 + RAG | 30h（50%） |
| 前端 | 8h（13%） |
| 文档 | 12h（20%） |
| 测试调优 | 6h（10%） |
| 缓冲 | 4h（7%） |
| 云端预算 | DashScope 免费额度足够（预估 embedding < 50 万 token，chat < 30 万 token）；Ollama 本地零成本 |
| 本地环境 | Docker 起 MySQL 8（`docker run` 一行），避免安装污染；Chroma 落 `./data/chroma` |

### 7.2 AI 编程工具分工（题目要求说明使用体会，需刻意记录）

| 环节 | 是否用 AI 生成 | 说明（可直接写进项目说明.md） |
|---|---|---|
| CRUD 接口、Pydantic 模型、前端页面 | ✅ 大量使用 | 模板化代码，AI 生成后人工核对字段 |
| SQL 建表脚本、ER 图 | ✅ 使用 | 生成后手工调整索引与约束 |
| **RAG 检索链路、Prompt 模板、上下文预算** | ❌ 手写为主 | 这是评分核心，必须自己掌控；AI 只作为方案讨论对象 |
| **SSE 流式管道** | ⚠️ 半自动 | AI 初版常犯错：忘记 `X-Accel-Buffering`、事件间少空行、异常时不 close 流——需人工修正（这些"修正点"就是文档里的加分素材） |
| 文档初稿 | ✅ 使用 | 但技术决策与实验数据必须自己填 |

> **刻意留痕**：开发过程中把每一处"AI 生成有问题 → 我怎么改的"记进 `NOTES.md`，Day 5 直接转写为《项目说明.md》中"对 AI 生成代码做了哪些优化和修正"章节。这是题目明确的重点提问，临时编造会很空洞。

### 7.3 若为 2 人配置

- A（主力）：RAG Core + 流式 + 后端 API + AI 架构文档
- B（辅助）：数据库 + 用户会话模块 + 前端三页面 + API 文档 / 运行指南
- 接口契约 Day 1 下午定稿，B 用 Mock 先行，Day 4 上午联调。

---

## 八、风险评估与应对

按 **风险值 = 概率 × 影响** 排序，前 5 项为需要主动预防的重点。

| # | 风险 | 概率 | 影响 | 触发信号 | 应对策略（预防 / 补救） |
|---|---|---|---|---|---|
| R1 | LLM API Key 失效 / 免费额度耗尽 / 触发限流 | 高 | 致命 | 调用返回 429 或 401 | **预防**：Day 1 即接入 `LLMProvider` 抽象 + Ollama 本地兜底；调用层加指数退避重试（3 次）。**补救**：`.env` 切 `LLM_PROVIDER=ollama` 一键降级 |
| R2 | Day 3 结束 SSE 流式仍未跑通 | 中 | 致命 | Day 3 晚仍在调 | **预防**：Day 1 先写 20 行 SSE Demo 验证前后端链路。**补救**：Day 4 上午全力攻坚；实在不通则退化为 WebSocket（题目允许） |
| R3 | 时间超支，Day 5 文档来不及 | 高 | 严重 | Day 4 中午 P0 未全绿 | **预防**：Day 4 中午硬 Freeze；文档分散到 Day 1/2 晚上提前写。**补救**：砍掉全部 P2，优先保 D3~D8 六份文档 |
| R4 | 中文切分效果差导致检索命中率低 | 中 | 严重 | 评测集命中率 < 60% | **预防**：不用固定字符数硬切，采用「段落→句号→逗号」递归切分 + 80 字重叠。**补救**：调小 chunk 到 300 字、加大 Top-K、降低阈值到 0.3 |
| R5 | MySQL 与 Chroma 数据不一致（删文档后向量残留） | 中 | 中 | 删除后仍能检索到 | **预防**：`kb_chunks.vector_id` 建立映射；删除走 `deleting` 中间态。**补救**：启动时对账任务清理孤儿向量 |
| R6 | Embedding 模型本地下载慢 / 无 GPU | 中 | 中 | Day 1 下载超 30 分钟 | **预防**：主用云端 embedding；本地兜底选 `bge-small-zh-v1.5`（仅 95MB，CPU 可跑） |
| R7 | PDF 解析乱码 / 失败 | 中 | 低 | 上传 pdf 后 status=failed | 题目中 PDF 为**可选项**：捕获异常写入 `error_msg`，前端展示失败原因，不影响主流程 |
| R8 | Nginx / 代理缓冲导致流式"假卡顿" | 中 | 中 | 后端有流、前端整段出现 | 响应头加 `X-Accel-Buffering: no` + `Cache-Control: no-cache`；开发期直连 Uvicorn 不走代理 |
| R9 | 真实 API Key 被提交进 Git | 中 | 致命（直接扣分） | — | Day 1 就配 `.gitignore` 忽略 `.env`；提交前 `git log -p \| grep -iE "sk-\|api[_-]key"` 全量扫描 |
| R10 | 上下文超长导致 LLM 报错 | 中 | 中 | 400 context length exceeded | ContextBuilder 做硬性 Token 预算（用 tiktoken 或字符数近似），超限自动截断并标注 |
| R11 | 面试官环境跑不起来 | 中 | 严重 | — | Day 5 晚净环境验收（换目录 clone 重来一遍）；提供 `docker-compose.yml` 起 MySQL；预置数据自动初始化 |
| R12 | 沉迷前端美化 / 管理后台，挤占核心分 | 中 | 严重 | Day 4 还在调 CSS | 记住：前端在评分表中**无独立分项**。用 AntD 默认样式，一行自定义 CSS 都不写 |
| R13 | 邮件遗漏院校+姓名备注 | 低 | 中 | — | 提交检查清单第一条 |

---

## 九、加分项攻坚方案（差异化关键）

这两项是本题拉开差距的地方，且**均允许以设计文档形式交付**，投入产出比极高。

### 9.1 大规模知识检索下的 LLM 执行保障

**问题定义**：当召回片段/业务规则达到数十条时，LLM 出现两类失效——① 注意力稀释（中间位置内容被忽略，即 "Lost in the Middle"）② 信息过载幻觉（编造规则、混淆不同规则）。

**四层防护设计**：

| 层 | 机制 | 做法 |
|---|---|---|
| L1 | **分级召回 + 重排** | Top-K 粗召回 30 条 → 交叉编码器/LLM 打分重排 → 精选 8 条。避免一次性把 30 条塞进去 |
| L2 | **规则优先级排序 + 位置编排** | 给每条知识片段打 `priority`（强制规则 > 业务政策 > 一般描述）。组装时采用 **"三明治布局"**：最高优先级规则放 Prompt **最前**和**最后**各一份（首尾注意力最强），中部放补充材料。这是对 Lost-in-the-Middle 的直接工程对抗 |
| L3 | **分层摘要（Map-Reduce）** | 片段数 > 阈值时，先按文档分组做并行摘要（Map，每组保留原文编号），再把摘要 + 高优先级原文一起送入生成（Reduce）。摘要阶段强制"只压缩不推理，保留所有数字、期限、金额原文" |
| L4 | **分步校验（Verify Pass）** | 生成完成后，抽取回答中的所有事实断言（数字/期限/条件），用一次轻量 LLM 调用做 NLI 式校验：「以下断言是否被参考资料支持？」不被支持则触发重生成或降级为"该信息未在知识库中明确说明" |

**结构化编号约束**：每个片段以 `[K1] [K2] ...` 编号注入，System Prompt 要求回答必须以 `[K编号]` 标注依据。后处理校验编号合法性——出现不存在的编号即判定为幻觉，直接触发重试。这是一个**可自动检测幻觉**的低成本机制。

**效果验证方式**（文档中必须写）：
- 构造 30 条规则的压力知识库，其中埋 3 条"关键规则"分别置于首/中/尾
- 对比开启/关闭 L2+L3 时，中部规则的命中率变化
- 指标：关键规则命中率、编号越界率（幻觉代理指标）、拒答正确率
- 给出前后对比表格，即使样本量小也远胜空谈

### 9.2 终极挑战：Agent 需求任务拆解

**目标**：Agent 收到需求（如"用户下单后自动发送短信通知"）+ 全套技术/接口文档，判断 ① 改哪些微服务 ② 哪些可并行 ③ 哪些有先后依赖。

**六步流水线**：

```
① 系统知识索引（离线一次性）
   把每个微服务的【职责描述 / 对外API / 发布的事件 / 订阅的事件 / 数据表 / 上下游依赖】
   构建为 双索引：向量索引（语义召回） + 结构化服务依赖图 G(V,E)

② 需求语义解析
   LLM 抽取结构化意图：{触发事件, 动作, 参与实体, 数据需求, 时序约束}
   例："用户下单后自动发送短信通知"
     → 触发: 订单创建成功事件; 动作: 发送短信; 实体: 用户手机号, 订单信息

③ 影响面召回（Recall）
   向量检索 + 依赖图 1-hop 扩散，得到候选服务集
   → {订单服务, 通知服务, 用户服务, 短信网关, 前端}

④ 改动点判定（Judge）
   逐服务提问 LLM，输出结构化 JSON：
   { service, need_change: bool, change_type: [新增接口|新增事件消费|修改逻辑|配置],
     provides: ["notify.sendSms"], requires: ["user.getPhone"], reason, confidence }
   ⚠️ 逐服务独立判定（而非一次性输出全部），避免长上下文注意力稀释

⑤ 依赖 DAG 构建 + 拓扑分层
   建边规则：
   - 契约依赖：A.requires ∩ B.provides ≠ ∅  ⇒  B → A（接口提供方先改）
   - 数据依赖：A 读取 B 新增的字段        ⇒  B → A
   - 事件依赖：消费者需先具备处理能力      ⇒  Consumer → Producer
   环检测：若成环 → 拆分为「先加接口(向后兼容) / 再改调用 / 最后清理」三阶段破环
   拓扑排序 → 同层无边即可并行

⑥ 输出执行计划 + 人工确认
   分层任务卡：层内并行、层间串行，每张卡含 服务/改动类型/接口契约/验收标准
```

**示例输出（"用户下单后自动发送短信通知"）**：

| 层级 | 服务 | 改动内容 | 并行性 |
|---|---|---|---|
| L1 | 用户服务 | 新增内部接口 `GET /internal/users/{id}/phone`（脱敏策略） | ✅ 与通知服务并行 |
| L1 | 通知服务 | 新增 `POST /notify/sms`；接入短信网关；幂等键设计 | ✅ 与用户服务并行 |
| L2 | 订单服务 | 下单成功后发布 `order.created` 事件 / 或调用通知服务 | ⛔ 依赖 L1 两个接口契约 |
| L2 | 通知服务 | 订阅 `order.created`，编排「查手机号 → 发短信 → 记录结果」 | ⛔ 依赖 L1 |
| L3 | 前端 | 用户中心「短信通知开关」设置项（可选） | ⛔ 依赖 L2 配置接口 |
| L3 | 全链路 | 联调 + 失败重试/死信队列验证 | ⛔ 最后 |

**判定准确性保障**：
- **契约先行**：只要 A 需要调用 B 的新接口，B 必先改 —— 这条规则可用代码硬判定，不依赖 LLM 主观判断
- **置信度门控**：`confidence < 0.7` 的服务标记为"待人工确认"，不直接进入执行队列
- **反向验证**：拆解完成后，让 Agent 反向回答"如果不改 X 服务，需求能否满足？"，用于剔除误报
- **落地形态**：可在本项目中以 `POST /api/agent/decompose` 最小接口实现（输入需求文本 + 服务文档，输出分层 JSON），有实现比纯文档再高一档

---

## 十、质量保障措施

### 10.1 RAG 质量评测（这是"AI 工程思维"25% 的直接体现）

构建 `eval/testset.jsonl`，**25-30 条**，五类覆盖：

| 类别 | 条数 | 期望行为 |
|---|---|---|
| 直接命中 | 8 | 答案正确 + 引用来源正确 |
| 跨片段综合 | 4 | 能整合 2 个以上片段 |
| **知识库外问题** | 5 | 必须走兜底话术，**不得编造** |
| 多轮指代 | 4 | Query Rewrite 后能正确检索 |
| 边界/异常 | 4 | 超 500 字、空输入、超配额均正确拦截 |

**评测指标**（人工打分即可，题目允许）：

| 指标 | 定义 | 目标 |
|---|---|---|
| 检索命中率 | Top-K 中含正确片段的比例 | ≥ 85% |
| 回答正确率 | 人工判定答案正确 | ≥ 80% |
| 引用正确率 | 展示的来源确为答案依据 | ≥ 90% |
| **拒答正确率** | 库外问题正确兜底的比例 | **100%（红线）** |
| 编号越界率 | 引用了不存在的片段编号 | 0% |
| 首 Token 延迟 | 请求到第一个 delta | < 2s |

跑两轮：Prompt v1 与 v2 各一轮，**产出对比表格写进文档**，这就是"Prompt 经过明显优化，能有效减少幻觉（需说明优化思路）"这一加分项的实证。

### 10.2 代码质量

- 分层强制：`api / service / rag / repository / model`，禁止 API 层直连数据库
- 工具链：`ruff` + `black`（后端），`eslint` + `prettier`（前端）
- **RAG Core 每个类头部写"这一步在做什么、为什么这样设计"的中文注释** —— 评分表明确要求"关键逻辑有注释"
- 异常处理清单（逐条自查）：LLM 超时 / LLM 限流 / LLM 返回空 / Embedding 失败 / 向量库不可用 / 文件解析失败 / 文件超限 / Token 超长 / 配额超限 / JWT 失效
- 单元测试（≥10 个，覆盖高风险纯函数）：切分器、Token 预算、阈值过滤、配额自增、Prompt 组装、引用编号校验

### 10.3 交付质量

| 检查项 | 方式 |
|---|---|
| 净环境可启动 | 换空目录 `git clone` → 严格按《运行指南.md》操作，全程不看源码 |
| 冷启动可问答 | 启动后不做任何操作，直接提问，预置知识应能命中 |
| 无密钥泄漏 | `git log -p` 全量搜索 `sk-`、`api_key`、`ACCESS_KEY` |
| `.env.example` 完整 | 每个变量都有注释说明用途与获取方式 |
| 文档链接有效 | 所有 md 内的相对路径可点开 |
| Mermaid 图可渲染 | 在 GitHub 页面上实际查看渲染效果 |

---

## 十一、提交前最终检查清单（Day 5 22:00 执行）

```
□ backend/ 可独立启动，README 含 API Key 配置说明
□ frontend/ 可独立启动，README 完整
□ 数据库初始化脚本存在且可执行（建表 + 初始数据）
□ docs/API文档.md —— 含登录/上传/问答(流式)/会话历史/反馈 5 类接口 + 示例 + SSE 格式
□ docs/数据库设计.md —— ER 图 + 表结构
□ docs/AI架构设计.md —— RAG 流程图 + Prompt 模板 + 阈值/TopK 依据
□ docs/业务流程说明.md —— 完整链路图
□ 项目说明.md —— 选型理由 / 架构图 / AI 工程三问 / AI 工具使用体会（含"我修正了什么"）
□ 运行指南.md —— 从零到跑通的完整步骤
□ .env.example 存在，真实 Key 不在仓库任何一次提交中
□ 预置 2-3 篇文档，启动即就绪
□ 流式输出实测逐字显示
□ 空检索兜底实测不编造
□ 加分文档：大规模检索执行保障设计.md、Agent任务拆解设计.md
□ 仓库 Public 或已授权面试官访问
□ 邮件发至 xzjl@haici.com，标题/正文含【院校 + 姓名】+ 仓库链接
```

---

## 十二、进度追踪表（每日 22:00 自评，可直接复制使用）

| 里程碑 | 计划完成 | 判定标准 | 实际状态 | 偏差处置 |
|---|---|---|---|---|
| M1 环境与设计定稿 | D1 23:00 | LLM/Embedding/Chroma 三验证通过；`init_db.sql` 完成 | ☐ | 延误 → D2 上午补，并砍掉全部 P2 |
| M2 知识入库链路 | D2 23:00 | 上传→ready→可检索；删除→向量清除 | ☐ | 延误 → D3 上午补，前端降级为最简页面 |
| M3 RAG + 流式 | D3 23:00 | `curl -N` 可见 SSE 逐字；引用来源正确 | ☐ | 延误 → D4 全天攻坚，放弃全部 P1 |
| M4 前端联调 + 加分 + 调优 | D4 23:00 | 浏览器逐字渲染；意图/追问可见；调优数据成表 | ☐ | 延误 → 保主线，P1 只写文档不写码 |
| M5 文档与提交 | D5 23:00 | 检查清单全绿；邮件已发 | ☐ | 不可延误 |

**每日站会自问三题**（写进 `NOTES.md`）：
1. 关键路径今天推进到哪一步？是否落后于计划？
2. 今天遇到的最大不确定性是什么？是否已经引爆并解决？
3. 有没有在做评分表上不给分的事？（如美化 UI、过度抽象）

---

## 附：一句话总结

**用 Day 1 引爆所有外部不确定性，用 Day 2-3 死磕 65 分的 RAG 主链路，Day 4 中午果断 Freeze，Day 5 用文档把"我懂原理"讲透——尤其是两道硬核加分题，它们只要一份好文档，却是拉开差距的地方。**
