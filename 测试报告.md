# AI 智能客服系统（RAG）— 功能测试与缺陷修复报告

- **测试对象**：`C:/Users/25837/Desktop/5day/backend`（FastAPI + Chroma + MySQL + DashScope）
- **测试环境**：Windows，MySQL 8.4.7（3306 / root / 123456 / 库 `ai_customer_service`），Python 3.13 venv，`DASHSCOPE_API_KEY=sk-1f4c…`（有效）
- **测试方式**：自动化 API 测试套件（`test_api.py`，14 项）+ 检索/引用链路专项验证
- **最终结论**：**14/14 项全部通过**，RAG 检索、多轮对话、引用来源、反馈、限流边界均验证可用

---

## 一、测试结果总览

| 编号 | 测试项 | 结果 | 说明 |
|------|--------|------|------|
| 1 | 用户注册 | ✅ PASS | 密码经 bcrypt 哈希入库 |
| 2 | 用户登录获取 JWT | ✅ PASS | 返回 `access_token` |
| 3 | 密码错误返回 401 | ✅ PASS | 鉴权边界正确 |
| 4 | 无 token 访问受保护接口 401 | ✅ PASS | 鉴权边界正确 |
| 5 | 创建会话 | ✅ PASS | |
| 6 | 上传知识文档 | ✅ PASS | |
| 7 | 异步向量化并轮询 ready | ✅ PASS | 文档状态正确流转 |
| 8 | RAG 问答（退换货政策） | ✅ PASS | 返回知识库相关答案 + 引用来源 |
| 9 | 多轮对话（会员折扣追问） | ✅ PASS | **修复后**由真实检索命中（score≈0.73），非兜底 |
| 10 | 空检索兜底 | ✅ PASS | 知识库无相关内容时返回兜底文案 |
| 11 | 超长问题（>500 字） | ✅ PASS | 业务规则生效 |
| 12 | 对话反馈提交 | ✅ PASS | **修复后**可正确关联到 assistant 消息 |
| 13 | 删除文档 | ✅ PASS | |
| 14 | 删除会话 | ✅ PASS | |

**SSE 流式 + 引用来源专项验证**：聊天事件流为 `start → content(分片) → done(sources)`，
`done` 事件携带 `sources: [{"doc_id":5,"doc_name":"kb_test.txt","chunk_id":"doc_5_chunk_0","score":0.7316}]`，
余弦相似度 0.73 证明余弦空间修复已生效，引用来源功能正常。

---

## 二、缺陷清单（按优先级）

### 🔴 P0 — 阻断性缺陷（导致系统崩溃 / 核心链路不可用）

#### P0-1 `init_db.sql` 列级 COMMENT 语法错误 → 建库失败
- **问题描述**：建表语句使用 `COMMENT='text'`（带等号），但 MySQL 列级注释语法为 `COMMENT 'text'`（无等号，仅表级可用 `COMMENT=`）。脚本执行直接报错，`ai_customer_service` 库无法建立。
- **复现步骤**：`mysql -uroot -p ai_customer_service < backend/init_db.sql` → 抛出 `ERROR 1064 (42000)` 语法错误，建表中断。
- **建议修复（已实施）**：全局将列级 `COMMENT='x'` 改为 `COMMENT 'x'`；文件顶部增加 `SET NAMES utf8mb4;`，避免中文默认值/注释乱码。

#### P0-2 ORM 模型缺少时间戳列 → 应用启动即崩溃
- **问题描述**：SQLAlchemy 模型 `user / session / message / kb_document / kb_chunk / feedback / usage_quota` 未定义 `created_at` / `updated_at` 列，但模型内 `Index(...)` 又引用了这些列名，启动时抛 `ConstraintColumnNotFoundError: no column named 'created_at'`，FastAPI 进程退出，所有接口 500。
- **复现步骤**：`python -m uvicorn app.main:app` → 启动日志 `ConstraintColumnNotFoundError`，进程退出。
- **建议修复（已实施）**：按 `init_db.sql` 逐表补 `created_at`（`DateTime, server_default=func.now()`）、`updated_at` 列，并复原对应索引（如 `idx_user_updated(user_id, updated_at)`、`idx_session_created(session_id, created_at)` 等）。

#### P0-3 passlib + bcrypt 4.x + Python 3.13 不兼容 → 注册/登录全 500
- **问题描述**：`passlib[bcrypt]` 在 Python 3.13 下调 bcrypt 4.x 时报 `ValueError: password cannot be longer than 72 bytes`（passlib 将「密码+salt」再次传入 bcrypt，超出 72 字节上限）。注册、登录接口全部 500。
- **复现步骤**：调用 `POST /api/auth/register` 或 `/login` → 500，日志 `password cannot be longer than 72 bytes`。
- **建议修复（已实施）**：弃用 passlib 封装，直接使用 `bcrypt` 库 `hashpw` / `checkpw`（密码编码为 utf-8 后哈希）；`requirements.txt` 改为 `bcrypt` 并移除 `passlib[bcrypt]`。

#### P0-4 Chroma 集合重复创建报错 → 聊天接口崩溃
- **问题描述**：旧代码用 `get_collection` + `create_collection`（裸 `except`）获取集合，当集合已存在时 `create_collection` 抛 `Chroma errors: Collection [knowledge_base] already exists`，聊天链路中断。
- **复现步骤**：上传并向量化一篇文档后再次初始化/聊天 → 聊天接口 500，日志 `... already exists`。
- **建议修复（已实施）**：改用 Chroma 原生 `get_or_create_collection()`，幂等获取集合。

#### P0-5 LLM 流式解析空 `choices` 越界 → 助手消息丢失 + 误触发 Ollama 兜底
- **问题描述**：`chat_stream` 中 `if chunk.choices[0].delta.content` 在流式最后一个 usage/[DONE] 分片（`choices == []`）时索引越界，抛 `IndexError: list index out of range`。文本已发送给前端，但异常在 `save_assistant_message()` **之前**抛出，导致：① 数据库无 assistant 消息；② 错误触发 Ollama 兜底（本地无 Ollama → 连接失败）；③ 反馈接口（项 12）因找不到 assistant 消息失败。
- **复现步骤**：任意一次聊天 → 后端日志 `LLM chat stream failed: list index out of range` + `Chat stream failed: Connection error.`；反馈接口返回「未找到 assistant 消息」。
- **建议修复（已实施）**：遍历分片时先 `if not chunk.choices: continue`，再取 `chunk.choices[0].delta`，与 `chat()` 非流式分支保持一致。

#### P0-6 Chroma 默认 L2 距离，retriever 按余弦换算 → 检索阈值失效
- **问题描述**：Chroma 集合默认空间为 L2，而 `retriever` 用 `similarity = 1 - distance` 当余弦相似度，并以余弦式阈值（0.6，命中不足降 0.3）过滤。L2 下该换算严重低估相似度，相关片段被错误过滤——强相关项（退换货）靠 0.3 兜底勉强命中，代词追问（会员折扣）直接走兜底，RAG 质量不稳定。
- **复现步骤**：多轮问「那会员折扣呢？」（知识库明确含会员折扣）→ 修复前返回兜底文案而非检索结果。
- **建议修复（已实施）**：创建集合时显式 `metadata={"hnsw:space": "cosine"}`，使 `distance` 为余弦距离，阈值按预期生效（验证 score≈0.73）。

---

### 🟠 P1 — 功能缺陷（影响核心业务流程）

#### P1-1 多轮对话检索只用最新一条消息 → 代词追问检索失败
- **问题描述**：`retriever` 仅用 `request.message`（最新一条）做 embedding 检索。当上一轮问「退换货政策」、本轮问「那会员折扣呢？」时，单凭代词句语义与「会员等级权益」片段相似度低，检索落空。
- **复现步骤**：同一会话连续提问，第二轮代词追问命中率显著下降，返回兜底。
- **建议修复（已实施）**：在 `chat_service` 组装检索 query 时拼接最近一条用户历史消息（如「上文：{prev_user} 当前问题：{cur}」），提升代词消解能力。验证「会员折扣」追问命中 score≈0.73，返回真实会员权益内容。

---

### 🟡 P2 — 健壮性 / 质量改进

#### P2-1 Pydantic v2 严格类型：`datetime` 字段被塞字符串 → 500
- **问题描述**：`auth.py` / `feedback.py` schema 的 `created_at: str`，但 ORM 返回 `datetime` 对象，Pydantic v2 严格校验失败 → 500。
- **建议修复（已实施）**：改为 `created_at: datetime`（补 `from datetime import datetime`）。

#### P2-2 陈旧零向量污染集合
- **问题描述**：早期用错误 key（`sk-b5287…` → 401）跑过一次，`embed_batch` 的 `except` 把嵌入静默转成零向量入库，污染检索（命中率恒为 0）。
- **建议修复（已实施）**：删除 stale 的 `chroma` 目录并以正确 key 重建。**根因建议**：`embed_batch` 失败时应 `raise` 而非返回零向量，避免静默污染（已在测试过程中规避，后续可加固）。

#### P2-3 DashScope key 被沙箱环境变量覆盖
- **问题描述**：沙箱预设 `DASHSCOPE_API_KEY=sk-b5287...` 覆盖了 `.env` 中有效 key，导致 embedding/LLM 返回 401（pydantic-settings 优先级：环境变量 > `.env`）。
- **临时修复（已实施）**：启动后端时显式 `DASHSCOPE_API_KEY=sk-1f4c... python -m uvicorn ...` 覆盖。
- **正式部署建议**：在部署环境正确配置环境变量，或确保 `.env` 未被更高优先级的 env 覆盖；不要将密钥提交进版本库。

---

## 三、前端缺陷（已修复代码，未并入本次后端提交）

前端存在 4 处已知问题并已修复（stale-closure、流式卸载取消、`inline style` 违规范、`request.ts` 重复 `controller` 声明），因本次任务聚焦于后端链路跑通与测试报告，前端改动留待单独提交。

---

## 四、遗留风险与后续建议

1. **`embed_batch` 失败应显式 raise**，避免再次出现零向量静默污染（P2-2 根因）。
2. **多知识库路由 / 意图识别 / 追问引导** 为题目可选扩展项，当前未实现，如需冲刺高分可补充。
3. **限流（每日 100 次）** 已在测试中覆盖边界（超长问题项），但正式压测建议补充并发场景。
4. **密钥管理**：正式环境务必通过部署平台注入正确 `DASHSCOPE_API_KEY`，并避免提交 `.env`。
